---
layout: null
permalink: /feed.xml
sitemap: false
---
{%- assign visible_posts = site.posts | where_exp: 'p', 'p.hidden != true' -%}
{%- assign limit = site.feed.posts_limit | default: 30 -%}
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ site.lang | default: 'zh-Hant' }}">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" hreflang="{{ site.lang | default: 'zh-Hant' }}" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed.xml' | absolute_url | xml_escape }}</id>
  <title type="html">{{ site.title | smartify | xml_escape }}</title>
  <subtitle>{{ site.description | strip_newlines | xml_escape }}</subtitle>
  {%- if site.author -%}
  <author>
    <name>{{ site.author.display_name | default: site.author.name | xml_escape }}</name>
    {%- if site.author.email %}<email>{{ site.author.email | xml_escape }}</email>{%- endif %}
  </author>
  {%- endif -%}
  {%- for post in visible_posts limit: limit -%}
  <entry>
    <title type="html">{{ post.title | smartify | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    {%- if post.description %}<summary type="html">{{ post.description | xml_escape }}</summary>{%- endif %}
    {%- assign post_author_handle = post.author | default: site.author.name -%}
    {%- assign post_author = site.data.authors[post_author_handle] -%}
    {%- if post_author -%}
    <author>
      <name>{{ post_author.display_name | default: post_author.name | xml_escape }}</name>
    </author>
    {%- endif -%}
    {%- for cat in post.categories -%}
    <category term="{{ cat | xml_escape }}" />
    {%- endfor -%}
    {%- for tag in post.tags -%}
    <category term="{{ tag | xml_escape }}" />
    {%- endfor -%}
    {%- if post.image.path -%}
    <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ post.image.path | absolute_url | xml_escape }}" />
    {%- endif -%}
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}"><![CDATA[{{ post.content | strip }}]]></content>
  </entry>
  {%- endfor %}
</feed>
