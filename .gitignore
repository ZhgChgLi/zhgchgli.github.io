_site
.sass-cache
.jekyll-metadata
.jekyll-cache
vendor
.bundle
.DS_Store
Gemfile.lock

# _posts/ is a per-build artifact; bin/lang stages files from L10n/posts/<lang>/
_posts/*.md
_posts/*.markdown

/_posts/**

# _pages/ is a per-build artifact; bin/lang stages files from L10n/pages/<lang>/
/_pages/**

# photos.json is regenerated by CI before each build (see workflow).
assets/data/photos.json
assets/.DS_Store
.DS_Store

# Translator local venv (cache/ is tracked so re-runs reuse translations)
tools/translators/.venv/
